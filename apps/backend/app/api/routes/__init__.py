# Package: app.api.routes
# This __init__.py marks the 'routes' directory as a Python package.
# No code needed - just an empty file to enable imports like:
#   from app.api.routes.search import router