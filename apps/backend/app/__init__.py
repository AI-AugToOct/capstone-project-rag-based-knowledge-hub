# Package: app
# This __init__.py marks the 'app' directory as a Python package.
# No code needed - just an empty file to enable imports like:
#   from app.services import auth
#   from app.models import schemas