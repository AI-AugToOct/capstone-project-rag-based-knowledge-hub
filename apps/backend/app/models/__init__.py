# Package: app.models
# This __init__.py marks the 'models' directory as a Python package.
# No code needed - just an empty file to enable imports like:
#   from app.models.schemas import SearchRequest, SearchResponse